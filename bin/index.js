// #!/usr/bin/env node

// const main = require('../src/main');

// /*const [
//   ,, 
//   repoUrl, 
//   targetDir, 
//   searchString, 
//   replaceString, 
//   dbName, 
//   tableName
// ] = process.argv;

// if (!repoUrl || !targetDir || !searchString || !replaceString || !dbName || !tableName) {
//   console.error('Usage: git-duplicator <repo-url> <target-directory> <search-string> <replace-string> <db-name> <table-name>');
//   process.exit(1);
// }*/

// const name = await askQuestion('Enter your Project Name ');
// const connectionString = await askQuestion('Enter your SQL connection string ');

// //main(repoUrl, targetDir, searchString, replaceString, dbName, tableName);
// main(name, connectionString);
